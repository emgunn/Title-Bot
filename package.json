{
  "dependencies": {
    "bootstrap": "^5.1.3",
    "express": "^4.17.1",
    "metadata-scraper": "^0.2.44"
  },
  "scripts": {
    "install-server": "cd client && yarn install",
    "install-all": "npm install && npm run install-server",
    "start": "node server.js",
    "start-server": "cd client && yarn start",
    "start-all": "npm run start & npm run start-server"
  }
}
